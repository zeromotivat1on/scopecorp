- Use Unordered Access View (UAV) to implement mouse pick.

- Add sampled profiling zones for runtime stats.

- Use bindless/virtual textures to reduce gfx api calls.

- UI input functions for non-text are basically the same, cleanup.

- Separate aabb arrays for collision, editor visuals etc.

- Synchronize logging.

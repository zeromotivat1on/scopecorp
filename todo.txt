- Its not clear if there will be cases of iteration over all assets of some type,
  so maybe remove render/audio registry and let all assets reside in asset table.

- Synchronize logging.

- Enqueue draw command order is crucial for expected output, we need to add sort/priority
  of submitted draw commands. Also possible to add additional draw queues, e.g for ui.

- Implement sort of material instance system to handle same material
  usages and in particular uniform value updates in different entities.

- Fix ray overlap from mouse position in case of rotated camera.
